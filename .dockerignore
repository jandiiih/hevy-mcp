# Deny everything by default so local credentials and unrelated repository
# files are never sent to the Docker daemon or retained in its build cache.
**

# Docker must still be able to read the build definition and ignore rules.
!Dockerfile
!.dockerignore

# The container entrypoint translates platform environment variables into CLI
# flags, so the runtime stage cannot be built without it.
!entrypoint.sh

# Standalone build inputs.
!package.json
!package-lock.json
!tsconfig.base.json
!tsconfig.json
!packages/
!packages/hevy-client/
!packages/hevy-client/**
!packages/operations/
!packages/operations/**
!packages/core/
!packages/core/**
!packages/node/
!packages/node/**
!packages/worker/
!packages/worker/**
